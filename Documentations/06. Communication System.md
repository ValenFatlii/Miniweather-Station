# Communication System

## Overview

The Miniweather Station implements an IoT communication system to enable remote monitoring and data management. The communication architecture is designed using MQTT over WebSocket Secure (WSS) protocol to transmit environmental measurement data from the ESP32 controller to the Hyperbase monitoring platform.

The communication system consists of three main components:

1. ESP32 weather station node
2. MQTT communication infrastructure
3. Hyperbase dashboard and database platform

The system is designed with an offline-first approach, where measurement data is continuously stored locally using a microSD card before transmission. This approach ensures that environmental data remains available even when network communication is temporarily unavailable.

---

# Communication Architecture

![Communication Architecture](../images/communication_architecture.png)

The overall communication flow is:


The ESP32 acts as the edge device responsible for collecting, processing, and transmitting environmental data.

---

# Communication Protocol

## MQTT over WebSocket Secure (WSS)

The system uses MQTT protocol with WebSocket Secure transport for communication between the ESP32 and the cloud platform.

MQTT is selected because it provides:

- Lightweight communication
- Low bandwidth requirement
- Publish-subscribe architecture
- Reliable IoT data transmission


The use of WebSocket Secure provides:

- Encrypted communication
- Compatibility with web-based IoT platforms
- Secure data exchange through internet networks


---

# MQTT Communication Workflow

The data transmission process follows the sequence:


---

# Measurement and Transmission Cycle

The weather station operates using a 60-second measurement interval.

Each cycle consists of:


---

# Sensor Data Acquisition Strategy

Different sensors use different acquisition methods depending on their measurement characteristics.

## Periodic Digital Sensors

The following sensors are directly measured every 60 seconds:

| Sensor | Parameter |
|-|-|
| SCD40 | Temperature |
| SCD40 | Humidity |
| SCD40 | CO₂ concentration |
| BMP280 | Atmospheric pressure |
| BMP280 | Altitude |
| BH1750 | Light intensity |
| AS5600 | Wind direction |
| INA219 | Voltage, current, power |


---

## Dynamic Pulse-Based Sensors

Some parameters require continuous counting during the measurement interval.

### Wind Speed

The anemometer generates pulses based on rotational movement.

Measurement process:


The measured wind speed represents the average wind condition during the sampling interval.

---

### Rainfall Measurement

The tipping bucket rain gauge generates a pulse for each bucket tipping event.

Measurement process:

This method allows the system to measure accumulated rainfall while maintaining a fixed data transmission interval.

---

# Data Payload Structure

The weather station transmits measurement data using JSON format.

Example payload:

```json
{
  "data": {
    "co2": 450,
    "temperature": 28.5,
    "kelembapan": 72.4,
    "tekanan_udara": 1012.5,
    "altitude": 25.4,
    "intensitas_cahaya": 850,
    "ketinggian_air_laut": 25.4,
    "arah_angin": 180,
    "kecepatan_angin": 2.1,
    "curah_hujan": 0,
    "current": 0.25,
    "voltage": 12.3,
    "power": 3.08
  }
}
